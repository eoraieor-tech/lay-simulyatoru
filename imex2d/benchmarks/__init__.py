"""Etalon (benchmark) modelləri — deck oxuyucularını (`io`) və model
qurucusunu (`application`) birləşdirən kompozisiya qatı (Seans 38).

`app.py` kimi yuxarı səviyyədədir: `domain`/`simulation` bu paketi tanımır.
"""
