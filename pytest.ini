[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    performance: wall-clock benchmark tests (tens of seconds); run the fast subset with -m "not performance"
