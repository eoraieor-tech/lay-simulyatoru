# Audit — mövcud `lay-simulyatoru` kod bazası

**Tarix:** 10 sentyabr 2026
**Auditə verilən:** `lay-simulyatoru-main (13).zip` — 10 sentyabr 2026, 12:54
**Səbəb:** sahibkar yeni 3D 3-fazalı simulyator planı verdi; mövcud kodun
nə qədərinin işə yaradığını müəyyən etmək lazımdır.
**Metod:** statik təhlil + testlərin real icrası (fərziyyə ilə deyil).

---

## 1. Qısa nəticə

Kod bazası **"qarışıq və səhvli" deyil.** Ölçülmüş göstəricilər peşəkar
səviyyəni göstərir. Planın ən çətin komponentləri artıq mövcuddur.

Əsas problem **kod keyfiyyəti deyil**, iki qopuq bağlantıdır:

1. **MPFA-O yazılıb, doğrulanıb — amma mühərrikə qoşulmayıb.**
2. **Qaz fazası yazılıb, demək olar bitirilib — sonra silinib** (v69).

Yəni planınızdakı işin böyük hissəsi görülüb; qalan iş əsasən
**birləşdirmə**dir, sıfırdan yazma deyil.

---

## 2. Ölçülər

| Göstərici | Dəyər |
|---|---|
| Python kodu | **65 175 sətir** |
| Bundan test | **28 509 sətir** (96 fayl) |
| Sənədləşdirmə | 27 `.md` faylı |
| Toplanan test sayı | **1 995** |
| `imex2d/` paket sayı | 11 |

---

## 3. Kod sağlamlığı — ölçülmüş

| Siqnal | Say | Şərh |
|---|---|---|
| `TODO` | **0** | |
| `FIXME` | **0** | |
| `HACK` / `XXX` / `BUG` | **0** | |
| Çılpaq `except:` | **0** | |
| `@pytest.mark.skip` | **0** | |
| `@pytest.mark.xfail` | 1 | |
| `except Exception` | 37 | əsasən UI qatında, baxılmalıdır |

65 min sətirlik bazada sıfır `TODO`/`FIXME` nadir haldır. Bu, "sonra
düzəldərəm" borcunun yığılmadığını göstərir.

---

## 4. Arxitektura

Təmiz **hexagonal (onion)** quruluş. Asılılıq istiqaməti həmişə içəriyə:

```
ui ──> rendering ──┐
 │                 ├──> application ──> simulation ──> interfaces ──> domain
 └─────────────────┘
```

`domain` heç nədən asılı deyil. `app.py` composition root-dur — bütün
konkret siniflər yalnız orada bağlanır.

`ARCHITECTURE.md` (51 KB) köhnə arxitekturanın **niyə** atıldığını da
sənədləşdirir (model obyekti yox idi, geologiya UI-nin içində idi,
rendering hesablama ilə qarışmışdı). Bu, bilinçli refaktorinqdir.

---

## 5. Modul-modul vəziyyət

| Paket | Sətir | Vəziyyət |
|---|---|---|
| `domain/` | 6 226 | ✅ Corner-point (COORD/ZCORN), həndəsə, PVT, SCAL, vahidlər |
| `interfaces/` | 317 | ✅ Provider müqavilələri (ABC) |
| `application/` | 3 898 | ✅ İş axını, `.imx` layihə faylı, servislər |
| `simulation/` | 5 404 | ✅ IMPES + tam implicit (FIM), Nyuton, CPR, quyu modeli |
| `discretization/` | 1 699 | ⚠️ **MPFA-O — yazılıb, QOŞULMAYIB** |
| `geology/` | 8 253 | ✅ Kriging, variogram, SGS, fasiya, cross-validation |
| `io/` | 1 638 | ✅ GRDECL, Eclipse `.DATA` ixracı, OPM idxalı |
| `rendering/` | 2 071 | ⚠️ matplotlib ✅ / VTK ❌ (bax §7) |
| `reporting/` | 336 | ✅ PDF hesabat |
| `history/` | 1 283 | ✅ History matching, həssaslıq analizi |
| `ui/` | 4 540 | ⚠️ PyQt5, 12 tab — 3D hissəsi VTK-dan asılı |

---

## 6. Planla müqayisə

| Planınızın tələbi | Vəziyyət |
|---|---|
| 3D corner-point grid | ✅ **var** — `corner_point_geometry.py`, 770 sətir |
| **MPFA-O** | ⚠️ **var, amma qoşulmayıb** — §8.1 |
| Geostatistik interpolyasiya | ✅ **var** — Kriging + SGS (RBF-dən güclüdür) |
| PVT (Bo, Bg, Rs, µ) | ⚠️ neft-su ✅, **qaz hissəsi silinib** — §8.2 |
| Stone II 3-fazalı relperm | ❌ **silinib** — §8.2 |
| Peaceman quyu indeksi | ✅ **var** |
| BHP / THP / VFP | ⚠️ BHP ✅ · **THP/VFP yoxdur** |
| IMPES | ✅ **var** |
| FIM (Nyuton + analitik Jakobian) | ✅ **var**, CPR ön-şərtçisi ilə |
| CFL ilə dinamik `dt` | ✅ **var** — `time_stepping.py` |
| STOIIP, RF, GOR, WaterCut | ⚠️ qismən — `reporting/` var, tam yoxlanmalı |
| PyVista 3D | ❌ yox — birbaşa VTK işlədilib (§7) |
| Plotly dashboard | ❌ yox — matplotlib işlədilib |
| 5-spot ssenarisi | ⚠️ `scenarios.py` var, 5-spot ayrıca yoxlanmalı |

---

## 7. Mühit problemləri

### 7.1 VTK bloklanır — TƏSDİQLƏNMİŞ

Windows 11 **Smart App Control** işlək vəziyyətdədir
(`VerifiedAndReputablePolicyState = 1`). VTK-nın DLL-ləri imzasızdır və
bloklanır:

```
ImportError: DLL load failed while importing vtkCommonCore:
An Application Control policy has blocked this file.
```

**Nəticə:** `rendering/vtk_volume.py` (895 sətir) və ondan asılı 3D
görüntü bu maşında işləmir.

### 7.2 İşlək mühit — sistem Python 3.14

Ölçülmüş: sistem Python 3.14-də SciPy-ın **bütün** modulları işləyir
(`sparse.linalg`, `interpolate`, `spatial`, `optimize`). Yalnız VTK
bloklanır. Layihə əlavə paket tələb etmir, çünki Kriging **öz içində**
yazılıb (PyKrige-a ehtiyac yoxdur).

Yeni yaradılan `venv`-lərdə isə SciPy-ın bir hissəsi bloklanır —
səbəb: yeni yazılan imzasız fayllar hələ reputasiya qazanmayıb.
**Tövsiyə: bu layihə üçün yeni venv yaratmayın, mövcud sistem Python
3.14 işlədin.**

---

## 8. Tapılan səhvlər

### 8.1 MPFA-O mühərrikə qoşulmayıb — ƏSAS BOŞLUQ

`imex2d/discretization/` (1 699 sətir, tam test edilib) **yalnız
testlərdən** idxal olunur. Heç bir işlək kod onu çağırmır.

Sübutlar:
- `simulation/discretization.py` başlığı: *"Future MPFA-O (HƏLƏ YOXDUR)"*
- `impes_engine.py::_reject_multipoint_impes()` — MPFA-O ötürülsə
  **qəsdən xəta atır**
- `implicit/jacobian.py::_build_pattern()` hər üz üçün **dəqiq 2
  hüceyrə** fərz edir; MPFA-O bir üzdə 2-dən çox hüceyrə bağlayır

**Qalan iş** (öz sənədinizdə dəqiq yazılıb): Jakobianın seyrəklik
naxışını 2-hüceyrəlidən N-hüceyrəli stensilə ümumiləşdirmək və axın
törəmələrini `grid.compute_flux()`-un öz Jakobianından almaq.

### 8.2 Qaz fazası silinib (v69)

`A7_PLAN.md` (44 KB) üç fazalı black-oil işinin **demək olar tam
bitirildiyini** sənədləşdirir: qaz PVT (Z-faktoru, Bg, Lee-Gonzalez-Eakin
lözlüyü), üç fazalı doyumluluq, **Stone II**, dəyişən keçidi (variable
switching), qalıq tənlikləri, **analitik Jakobian**, Nyuton döngəsi —
hamısı "HAZIR".

Həll olunmamış qalan: **quyu öz BHP həddinə yaxınlaşanda Nyuton rəqsi.**
Diaqnoz dəqiqdir — t≈6.7 gündə istismarçının bilavasitə qonşusunda neft
residualı DÖVR-2 rəqs edir (8·10⁻⁴ ↔ 4.5·10⁻³).

Bunun üzərinə v69-da **strateji dönüş** edilib: fizika OPM Flow-a həvalə
edilsin, proqram görüntü platforması olsun (`OPM_IMPORT.md`).

**Qiymətləndirmə:** bu səhv sirli deyil. Quyu BHP həddinə çatanda faza
yaranıb-yox olması ilə birlikdə Nyuton rəqsi rezervuar simulyasiyasında
tanınmış problemdir. Ədəbiyyatda həlli var: Appleyard chopping, doyma
dəyişiminin məhdudlaşdırılması, axın funksiyaları üçün trust-region
Nyuton (Wang & Tchelepi). Öz qeydlərinizdə də düzgün istiqamət
göstərilib (per-cell qəbul meyarı, trust-region).

**Bərpa: ✅ TAM MÜMKÜNDÜR — KOD TAPILDI.**

Kod 14 zip-in heç birində yox idi (silinmə mərhələli olub, bütün
arxivlər ondan sonrakıdır), lokal git klonu da tapılmadı. Lakin GitHub
reposu (`github.com/eoraieor-tech/lay-simulyatoru`) **publicdir** və
tarixçə tamdır.

Reponun **ilk commit-i** məhz bu məqsədlə saxlanılıb:

```
66deca8  v67 - son 3 fazali versiya (qaz fazasi + matplotlib 3D)
```

Həmin commit-dən çıxarılan **12 fayl, 4 118 sətir**:

| Fayl | Sətir |
|---|---|
| `imex2d/simulation/implicit/three_phase_residual.py` | 1 066 |
| `imex2d/simulation/implicit/three_phase_newton.py` | 385 |
| `imex2d/simulation/implicit/three_phase_engine.py` | 261 |
| `imex2d/simulation/stone_relperm.py` | 172 |
| `imex2d/simulation/implicit/three_phase_state.py` | 170 |
| `imex2d/domain/three_phase.py` | 111 |
| **Mənbə cəmi** | **2 165** |
| `tests/test_three_phase_residual.py` | 964 |
| `tests/test_three_phase_newton.py` | 257 |
| `tests/test_three_phase.py` | 215 |
| `tests/test_gas_pvt.py` | 192 |
| `tests/test_stone_relperm.py` | 192 |
| `tests/test_gas_ui_wiring.py` | 133 |
| **Test cəmi** | **1 953** |

Stone II implementasiyası düzgündür — standart düstur, üstəlik
**analitik törəmələri ilə** (`∂kro/∂Sw`, `∂kro/∂Sg`), yəni Jakobiana
qoşulmağa hazırdır:

```
kro = kro_end · [(krow/kro_end + krw) · (krog/kro_end + krg) − krw − krg]
```

Kod bərpa edilib saxlanılıb (bax `ISH_HESABATI.md` — Seans 2).

### 8.3 `pytest.importorskip("vtk")` işləmir — TEST DƏSTİNİ DAYANDIRIR

`tests/test_vtk_corner_point.py:40` və `tests/test_vtk_volume.py:12`:

```python
vtk = pytest.importorskip("vtk", reason="VTK quraşdırılmayıb")
```

Məqsəd "VTK yoxdursa ötür"dür. Lakin pytest 8.2-dən sonra
`importorskip` **yalnız `ModuleNotFoundError`-da** ötürür. Burada VTK
quraşdırılıb, sadəcə DLL-i bloklanıb — bu isə `ImportError`-dur.

**Nəticə:** toplama mərhələsində 2 xəta → **1 995 testin heç biri
işləmir**, pytest exit kodu 2 ilə dayanır.

**Düzəliş:**

```python
try:
    import vtk
except ImportError as exc:
    pytest.skip(f"VTK əlçatan deyil: {exc}", allow_module_level=True)
```

### 8.4 Uğursuz test — DİAQNOZ EDİLDİ VƏ DÜZƏLDİLDİ

`test_phase_b_production_integration.py::test_differential_phase_a_vs_phase_b_permx_differ_and_why`

```
geology_service.py:656
AttributeError: 'GeologicalGridSpec' object has no attribute 'cell_centroid'
```

**Mühüm nüans: səhv işlək kodda DEYİL, testin özündədir.**

`_cell_centres(grid, geometry)` imzası `CellGeometry` gözləyir. Hər iki
işlək çağırış düzgündür (`geology_service.py:533` və `:568` — ikisi də
`geometry` ötürür). Səhv yalnız burada:

```python
# tests/test_phase_b_production_integration.py:346  (SƏHV)
targets = WellBasedGeologicalModelBuilder._cell_centres(model_b.grid, spec)

# DÜZGÜN
targets = WellBasedGeologicalModelBuilder._cell_centres(model_b.grid, model_b.geometry)
```

**Yoxlanılıb:** düzəlişdən sonra `tests/test_phase_b_production_integration.py`
→ **23 keçdi, 0 uğursuz** (4.63 san).

### 8.5 THP / VFP yoxdur

Planınız THP və VFP tələb edir. Kodda BHP var, **THP və VFP modulu
yoxdur** — yazılmalıdır.

---

## 9. Test icrasının nəticəsi — ÖLÇÜLMÜŞ

**Mühit:** sistem Python 3.14.7, pytest 9.1.1, `MPLBACKEND=Agg`,
`QT_QPA_PLATFORM=offscreen`. VTK test faylları kənarlaşdırılıb (§8.3).

```
1 failed, 1993 passed, 1 skipped, 1 xfailed  —  468.20s (7:48)
```

| Nəticə | Say |
|---|---|
| ✅ Keçdi | **1 993** |
| ❌ Uğursuz | 1 (§8.4 — düzəldildi) |
| ⏭️ Ötürüldü | 1 |
| xfail | 1 |

**§8.3 və §8.4 düzəldiləndən sonra gözlənilən nəticə: 1 994 keçən,
0 uğursuz.**

### Ən yavaş testlər

| San | Test |
|---|---|
| 40.6 | `test_facies_performance.py::test_benchmark_100x100_fast_search_beats_brute_force` |
| 34.3 | `test_polyhedral_geometry.py::test_constructing_and_validating_many_cells_scales_linearly` |
| 24.7 | `test_history_optimizer.py::test_differential_evolution_runs_within_its_budget` |
| 12.1 | `test_regression.py::test_five_spot_reference_case_reproduces_legacy_results` |

**Qeyd:** `test_five_spot_reference_case_reproduces_legacy_results` —
planınızın tələb etdiyi **5-spot ssenarisi üçün reqressiya testi artıq
mövcuddur** və keçir.

Bütöv dəstin 7-8 dəqiqə çəkməsi gündəlik iş üçün uzundur; gələcəkdə
`pytest-xdist` ilə paralelləşdirmə düşünülməlidir (4 nüvə / 8 thread).

---

## 10. Tövsiyə

Bu kod bazası **atılmamalıdır.** Sıfırdan yazmaq həm uzun, həm də
mövcud doğrulanmış fizikanı (Buckley-Leverett yaxınsaması, MPFA-O
testləri, 1 995 test) itirmək deməkdir.

Prioritet sıra:

| # | İş | Həcm | Vəziyyət |
|---|---|---|---|
| 1 | §8.3 — `importorskip` düzəlişi | ~4 sətir | diaqnoz hazır |
| 2 | §8.4 — testdəki `spec`→`geometry` | 1 sətir | ✅ **yoxlanılıb** |
| 3 | §8.2 — A7 kodunun bərpası | 4 118 sətir | ✅ **çıxarılıb** |
| 4 | §8.1 — MPFA-O-nun mühərriyə qoşulması | orta-böyük | plan var |
| 5 | Qaz fazasının Nyuton rəqsi | orta | həll yolu məlum |
| 6 | THP / VFP modulu (§8.5) | orta | sıfırdan |
| 7 | Vizualizasiya qərarı (§7.1) | — | ⏳ qərar |

1 və 2 kiçikdir və dərhal ediləcək işlərdir — test dəstinin sağlam
işləməsi qalan hər şeyin şərtidir.

⏳ **Sahibkarın strateji qərarı gözlənilir:**

1. **Öz fizikamız** (A7-ni bərpa edib bitirmək), yoxsa **OPM Flow**
   (v69 qərarına sadiq qalmaq)?
2. **Hansı repoda davam edirik?** Əsl layihə `lay-simulyatoru`-dur
   (52 commit, 65 175 sətir). Bu repo (`LAY-SIMULYATIR-MODELI-`)
   demək olar boşdur. İkisini paralel aparmaq səhvdir.
